'use strict';

(function() {

  function UserResource($resource) {
    return $resource('/api/users/:id/:controller', {
      id: '@_id'
    }, {
      changePassword: {
        method: 'PUT',
        params: {
          controller: 'password'
        }
      },
      resetDemoAccountBalance: {
        method: 'PUT',
        params: {
          controller: 'resetdemoaccountbalance'
        }
      },
      get: {
        method: 'GET',
        params: {
          id: 'me'
        }
      }
    });
  }

  angular.module('notteryApp.auth')
    .factory('User', UserResource);
})();
